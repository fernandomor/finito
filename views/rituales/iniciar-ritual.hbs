
<div class="mt-10 p-6 max-w-md mx-auto bg-white rounded-xl shadow-md flex items-center space-x-4">
  <div>
    <div class="text-xl font-medium text-black">{{ritualName}}</div>
    <p class="text-gray-500">Hora de inicio <b>{{hora}}</b> </p>
    <p class="text-gray-500">Hora de termino <b>
      {{#if horaFinal}}
        {{horaFinal}}
      {{/if}}
      {{#unless horaFinal}}
        : -
      {{/unless}}
      </b></p>
    <p class="text-gray-500">Nivel actual: <b>{{numActual}}</b></p>
    <p class="text-gray-500">Meta: <b>{{numMax}}</b></p>
  </div>
      {{#unless horaFinal}}
        <form action="/finalizar/{{_id}}" method="POST">
    <button>
      <div class=" p-4  w-40 h-30 bg-red-500 rounded-full shadow-md flex items-center">
      <p class="pl-4 text-white font-bold text-2xl">Finalizar</p>
    </div>
    </button>
  </form>
      {{/unless}}
</div>

<div class="flex">
<a class="mt-5 py-4 px-8  mx-auto text-sm font-medium rounded-md text-white bg-black hover:bg-yellow-400 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-yellow-500" href="/dashboard">Ver progreso diario</a>
</div>
  
 
